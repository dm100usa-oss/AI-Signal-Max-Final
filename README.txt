AI Answers Score — общий архив всех изменений

Последнее добавлено:
- Раздел "Основные параметры" теперь на языке интерфейса (был на английском при русском интерфейсе). Названия параметров берутся из текущего языка при отрисовке.

Всё остальное из этой сессии тоже внутри (6 уровней, стороны, блок Что это значит, ChatGPT-заглушка, категория бизнеса по Schema.org, язык результата, кнопки, отступы).

Файлы (заменить по этим путям):
- app/success/[mode]/page.tsx
- app/api/check/route.ts
- lib/analyze.ts
- locales/ru.ts
- locales/en.ts
